# 06. Principle: Versioned and Immutable

**Domain:** GitOps Principles (30%)

## Concept Refresher

Official: *Desired state is stored in a way that enforces immutability, versioning and retains a complete version history.*

Consequences:

- You can **see who changed what when** (audit)
- You can **rollback** by selecting a prior version
- You do **not** mutate tags that float (`:latest`, `main` as an image tag, unsigned moving aliases) as the *only* pin
- History is **append-only in practice** for the source of truth (revert = new commit)

OpenGitOps note: “use Git” is incomplete. **`image: nginx:latest`** in Git is still a Git commit, but the **system** is not versioned: you cannot recreate yesterday’s behaviour.

Immutability of **commits** (don’t rewrite `main`) plus immutability of **artifacts** (digest-pinned images).

## Question

**Q1.** Versioned and immutable requires:

- A. No history
- B. Complete version history of desired state
- C. Only the latest blob on a USB stick
- D. Wiki overwrite

**Q2.** `image: myapp:latest` as desired state:

- A. Perfectly immutable
- B. Breaks versioning of the running system; rollback of Git may not restore the same bits
- C. Is required by OpenGitOps
- D. Replaces Git

**Q3.** Prefer:

- A. Floating `latest`
- B. Digest (`@sha256:...`) or immutable semver tags produced by CI
- C. Untagged images
- D. Random tags per hour without record

**Q4.** `git commit --amend` + force-push on `main` as daily workflow:

- A. Strengthens complete history
- B. Weakens audit/immutability of the store
- C. Is Pulled Automatically
- D. Is reconciliation

**Q5.** This principle enables:

- A. Only faster DNS
- B. Traceability and rollback
- C. Disabling RBAC
- D. Push-only CD

**Q6.** A state store without history:

- A. Still GitOps
- B. Fails Versioned and Immutable
- C. Is better for drift
- D. Is Flux-only

**Q7.** Config in a Google Doc that people overwrite:

- A. Versioned immutable store
- B. Not an acceptable GitOps state store
- C. Continuously reconciled
- D. Declarative Kubernetes

**Q8.** Helm chart version `1.2.3` in Git plus image digest:

- A. Supports recreating that release
- B. Violates all principles
- C. Requires `:latest`
- D. Disables pull

**Q9.** Immutable desired state means the cluster never changes:

- A. True
- B. False — the cluster changes **by applying new versions** from the store
- C. True only for Flux
- D. True only for Argo

**Q10.** Who must not be able to silently rewrite history?

- A. Auditors
- B. Uncontrolled force-push on the production config branch
- C. The reconciler reading commits
- D. Signed tags

## Answers

**Q1.** B  
**Q2.** B  
**Q3.** B  
**Q4.** B  
**Q5.** B  
**Q6.** B  
**Q7.** B  
**Q8.** A  
**Q9.** B  
**Q10.** B

## Hands-on

Commit an image digest, then a `:latest` tag. `git log -p` shows why only the digest is a versioned artifact pin.

## Exam tips

- **`:latest` is the favourite trap.**
- History + immutability → **rollback and audit**.
- Cluster **does** change; **declarations** are versioned.
