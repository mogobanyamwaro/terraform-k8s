# 04. Steps, Loops, and Fan-out

**Domain:** Argo Workflows (36%) — Templates + data jobs

## Concept Refresher

A **steps** template is an invocator. It is a **list of lists**:

- Outer list = **sequence** (next group waits).
- Inner list = **parallel** steps in that group.

```yaml
- name: main
  steps:
  - - name: A
      template: echo
  - - name: B
      template: echo
    - name: C
      template: echo   # B and C in parallel after A
```

**Loops / fan-out**

- `withItems:` — iterate a list
- `withParam:` — iterate JSON array (often from a previous output parameter)
- `withSequence:` — numeric range

Each iteration is a child step. Combine with `when:` for conditionals (`{{steps.A.outputs.result}} == foo`).

**Steps vs DAG:** steps are naturally sequential groups. DAGs declare **dependencies** and maximise parallelism (see `07.md`). Nested steps can express a DAG, but DAG templates are the exam’s dependency graph.

## Question

**Q1.** In a steps template, items in the **same inner list**:

- A. Always run on different clusters
- B. Run in parallel
- C. Are Argo CD waves
- D. Are EventSources

**Q2.** Successive **outer** step groups:

- A. Run in parallel always
- B. Run sequentially
- C. Are CronWorkflows
- D. Require Helm

**Q3.** `withItems` is used to:

- A. Pin a Helm chart
- B. Fan-out a template over a list
- C. Shift canary traffic
- D. Create an AppProject

**Q4.** `withParam` typically consumes:

- A. An EventBus
- B. A JSON array (often a previous output parameter)
- C. Only PVCs
- D. Only Git tags

**Q5.** `when:` on a step:

- A. Deletes Git
- B. Conditionally skips/runs that step
- C. Is prune
- D. Is selfHeal

**Q6.** Steps are classified as:

- A. A template definition like container
- B. A template invocator
- C. An AnalysisTemplate
- D. An EventBus

**Q7.** Fan-out over files in a bucket is often:

- A. Argo CD ApplicationSet only
- B. A list/JSON parameter plus `withParam`, or a DAG of process tasks
- C. Blue-green
- D. Dex

**Q8.** Nested steps vs DAG:

- A. DAG cannot express dependencies
- B. DAG is the dedicated dependency-graph invocator; steps are grouped sequence/parallel
- C. Steps cannot run in parallel
- D. DAG is only for Argo CD

**Q9.** A CI “build then test then scan” pipeline maps cleanly to:

- A. Three outer step groups
- B. Three EventBuses
- C. Three Rollout pauses
- D. Three AppProjects

**Q10.** `withSequence` is:

- A. Git history
- B. A numeric loop helper
- C. Sync waves
- D. JetStream

## Answers

**Q1.** B  
**Q2.** B  
**Q3.** B  
**Q4.** B  
**Q5.** B  
**Q6.** B  
**Q7.** B  
**Q8.** B  
**Q9.** A  
**Q10.** B

## Hands-on

Hello workflow: step group 1 = one echo; group 2 = two parallel echoes. Then `withItems: [1,2,3]`.

## Exam tips

- **Inner list = parallel. Outer list = sequence.**
- `withParam` = data-driven fan-out (ETL).
