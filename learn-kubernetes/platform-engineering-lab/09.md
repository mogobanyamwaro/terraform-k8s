# 09. Blue-Green Rollout

**Domain:** GitOps and CD (25%)

## Question

In `cnpe-09`, Rollout `api`:

- `blueGreen.activeService: api-active`
- `previewService: api-preview`
- `autoPromotionEnabled: false`
- image `argoproj/rollouts-demo:blue`, 3 replicas

Create both Services. After a new image, preview must serve the new RS; active stays old until `promote`.

## Answer

```yaml
apiVersion: v1
kind: Service
metadata: {name: api-active, namespace: cnpe-09}
spec:
  selector: {app: api}
  ports: [{port: 80, targetPort: 8080}]
---
apiVersion: v1
kind: Service
metadata: {name: api-preview, namespace: cnpe-09}
spec:
  selector: {app: api}
  ports: [{port: 80, targetPort: 8080}]
---
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata: {name: api, namespace: cnpe-09}
spec:
  replicas: 3
  selector: {matchLabels: {app: api}}
  template:
    metadata: {labels: {app: api}}
    spec:
      containers:
      - name: demo
        image: argoproj/rollouts-demo:blue
        ports: [{containerPort: 8080}]
  strategy:
    blueGreen:
      activeService: api-active
      previewService: api-preview
      autoPromotionEnabled: false
```

```bash
kubectl -n cnpe-09 argo rollouts set image api demo=argoproj/rollouts-demo:yellow
kubectl argo rollouts promote api -n cnpe-09
```

## Verify

```bash
kubectl -n cnpe-09 get svc api-active -o yaml | yq '.spec.selector'
kubectl argo rollouts get rollout api -n cnpe-09
```

## Exam tips

- Preview selector updates first. Active updates on **promote**.
- AnalysisTemplate is extra credit if the task names Prometheus — `19.md` in CAPA theory; here attach `prePromotionAnalysis`.
